<div class="min-h-screen flex items-center justify-center bg-slate-100 p-4 -mt-20">
  <div class="max-w-md w-full bg-white rounded-3xl shadow-xl overflow-hidden border-t-8 border-yellow-400">
    
    <div class="p-8 text-center">
      <div class="relative mx-auto w-32 h-32 mb-4">
        <div class="absolute inset-0 bg-yellow-100 rounded-full animate-pulse"></div>
        <img src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/54.png" class="relative z-10 w-full h-full object-contain" alt="Psyduck Confuso">
      </div>

      <h2 class="text-2xl font-black text-slate-800 mb-2">Esqueceu a senha?</h2>
      <p class="text-slate-500 text-sm mb-6">Não se preocupe, acontece até com os melhores Mestres Pokémon. Vamos recuperar seu acesso.</p>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "space-y-4 text-left" }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :email, "Seu Email Registrado", class: "block text-sm font-bold text-slate-700 mb-1" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "ash@kanto.com", class: "w-full px-4 py-3 rounded-xl border border-slate-200 focus:border-yellow-500 focus:ring-yellow-500 bg-slate-50" %>
        </div>

        <%= f.submit "Enviar Instruções", class: "w-full py-3 px-4 bg-yellow-400 hover:bg-yellow-300 text-yellow-900 font-bold rounded-xl shadow-md transition-transform transform hover:-translate-y-1 cursor-pointer" %>
      <% end %>
      
      <div class="mt-6 pt-6 border-t border-slate-100">
        <%- if controller_name != 'sessions' %>
          <%= link_to "Voltar para Login", new_session_path(resource_name), class: "text-sm font-bold text-slate-400 hover:text-slate-600" %>
        <% end %>
      </div>
    </div>
  </div>
</div>