<!DOCTYPE html>
<html>
  <head>
    <title>PokeTodo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
  <div style="background-color: #333; color: white; padding: 10px; display: flex; justify-content: space-between; align-items: center;">
    <div>
      <strong>PokeTodo</strong>
    </div>
    
    <div>
      <% if user_signed_in? %>
        <span>Olá, <%= current_user.email %></span> |
        <span style="color: #ffd700; font-weight: bold; font-size: 1.2em;">
          ⭐ <%= current_user.points %> XP
        </span> |
        <%= link_to "Sair", destroy_user_session_path, method: :delete, style: "color: #aaa;" %>
      <% else %>
        <%= link_to "Entrar", new_user_session_path, style: "color: white;" %>
      <% end %>
    </div>
  </div>

  <div style="padding: 20px;">
    <% if notice %>
      <p style="color: green; background: #e6fffa; padding: 10px; border: 1px solid green;"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p style="color: red; background: #ffe6e6; padding: 10px; border: 1px solid red;"><%= alert %></p>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
